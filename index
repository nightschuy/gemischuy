<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SCHUY.XYZ | 3D Digital Art</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Playfair+Display:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    
    <!-- GSAP for Animations -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    
    <!-- Three.js for 3D Background -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>

    <!-- Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'vampire-black': '#090304',
                        'rebecca-purple': '#5D2896',
                        'old-silver': '#7E8489',
                        'puce-red': '#6A273D',
                        'dark-pastel-red': '#BF491E',
                        'off-white': '#E0E0E0',
                    },
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                        serif: ['Playfair Display', 'serif'],
                    }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #090304;
            color: #7E8489;
            overflow-x: hidden;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #090304; 
        }
        ::-webkit-scrollbar-thumb {
            background: #5D2896; 
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #BF491E; 
        }

        .glass-panel {
            background: rgba(9, 3, 4, 0.7);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(126, 132, 137, 0.1);
        }

        .text-outline {
            -webkit-text-stroke: 1px rgba(255,255,255,0.1);
            color: transparent;
        }

        .hover-image-container {
            overflow: hidden;
        }
        
        .hover-image {
            transition: transform 0.7s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }
        
        .hover-image-container:hover .hover-image {
            transform: scale(1.05);
        }

        /* Loader */
        #loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #090304;
            z-index: 9999;
            display: flex;
            justify-content: center;
            align-items: center;
            transition: opacity 0.5s ease;
        }
        
        .loader-content {
            text-align: center;
        }
    </style>
</head>
<body class="antialiased selection:bg-rebecca-purple selection:text-white">

    <!-- Loader -->
    <div id="loader">
        <div class="loader-content">
            <h1 class="font-serif text-4xl md:text-6xl text-white mb-4 tracking-tighter">SCHUY.XYZ</h1>
            <div class="w-48 h-1 bg-gray-800 mx-auto overflow-hidden relative">
                <div class="absolute top-0 left-0 h-full bg-rebecca-purple w-full animate-pulse"></div>
            </div>
        </div>
    </div>

    <!-- WebGL Background -->
    <canvas id="webgl-canvas" class="fixed top-0 left-0 w-full h-full z-0 pointer-events-none opacity-60"></canvas>

    <!-- Navigation -->
    <nav class="fixed top-0 left-0 w-full z-50 px-6 py-6 flex justify-between items-center mix-blend-difference text-white">
        <a href="#" class="font-serif text-2xl font-bold tracking-tighter">SCHUY</a>
        <div class="hidden md:flex space-x-8 font-sans text-sm tracking-widest uppercase">
            <a href="#work" class="hover:text-rebecca-purple transition-colors">Work</a>
            <a href="#about" class="hover:text-rebecca-purple transition-colors">About</a>
            <a href="#contact" class="hover:text-rebecca-purple transition-colors">Contact</a>
        </div>
        <button class="md:hidden text-white">
            <i data-lucide="menu"></i>
        </button>
    </nav>

    <!-- Main Content Wrapper -->
    <main class="relative z-10">

        <!-- Hero Section -->
        <section class="min-h-screen flex flex-col justify-center px-6 md:px-12 lg:px-24 pt-20">
            <div class="hero-text-container">
                <p class="text-rebecca-purple font-sans text-sm tracking-[0.3em] uppercase mb-4 reveal-text">Digital Artist & 3D Generalist</p>
                <h1 class="font-serif text-6xl md:text-8xl lg:text-[10rem] leading-[0.9] text-white mix-blend-overlay mb-8 reveal-text">
                    DIGITAL <br>
                    <span class="italic text-dark-pastel-red">REALITY</span> <br>
                    FORGED
                </h1>
                <div class="flex flex-col md:flex-row items-start md:items-center gap-8 mt-8 reveal-text">
                    <p class="max-w-md text-old-silver text-lg leading-relaxed">
                        Crafting immersive 3D environments, photorealistic renders, and abstract motion graphics. Based in the digital void.
                    </p>
                    <a href="#work" class="group flex items-center gap-2 text-white border border-white/20 px-6 py-3 rounded-full hover:bg-white hover:text-black transition-all duration-300">
                        Explore Portfolio
                        <i data-lucide="arrow-down-right" class="group-hover:rotate-45 transition-transform"></i>
                    </a>
                </div>
            </div>

            <!-- Floating Abstract Element (Visual interest) -->
            <div class="absolute right-0 top-1/4 w-1/3 h-1/2 hidden lg:block opacity-20 pointer-events-none">
                <div class="font-serif text-[20rem] leading-none text-outline rotate-90 origin-top-right absolute right-10">
                    3D
                </div>
            </div>
        </section>

        <!-- Work / Gallery Section -->
        <section id="work" class="py-24 px-4 md:px-12 lg:px-24 bg-gradient-to-b from-transparent to-vampire-black/90">
            <div class="flex justify-between items-end mb-16 border-b border-white/10 pb-4">
                <h2 class="font-serif text-4xl md:text-6xl text-white">Selected Works</h2>
                <span class="hidden md:block font-sans text-sm text-old-silver">2023 — PRESENT</span>
            </div>

            <!-- Gallery Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12">
                
                <!-- Project 1 -->
                <div class="project-card group cursor-pointer">
                    <div class="hover-image-container relative aspect-[4/5] w-full bg-gray-900 mb-4 rounded-sm overflow-hidden">
                        <!-- Placeholder for 3D Render -->
                        <img src="https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?q=80&w=1000&auto=format&fit=crop" alt="Abstract 3D Render" class="hover-image w-full h-full object-cover opacity-80 group-hover:opacity-100">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end p-6">
                            <span class="text-white font-sans text-sm tracking-widest uppercase border border-white/30 px-3 py-1 backdrop-blur-md">View Project</span>
                        </div>
                    </div>
                    <div class="flex justify-between items-baseline">
                        <h3 class="text-2xl text-white font-serif italic group-hover:text-rebecca-purple transition-colors">Neon Genesis</h3>
                        <span class="text-sm text-dark-pastel-red">Blender / Cycles</span>
                    </div>
                </div>

                <!-- Project 2 (Offset) -->
                <div class="project-card group cursor-pointer md:mt-24">
                    <div class="hover-image-container relative aspect-[4/3] w-full bg-gray-900 mb-4 rounded-sm overflow-hidden">
                        <img src="https://images.unsplash.com/photo-1633596683562-4a47eb4983c5?q=80&w=1000&auto=format&fit=crop" alt="Abstract Geometric Render" class="hover-image w-full h-full object-cover opacity-80 group-hover:opacity-100">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end p-6">
                            <span class="text-white font-sans text-sm tracking-widest uppercase border border-white/30 px-3 py-1 backdrop-blur-md">View Project</span>
                        </div>
                    </div>
                    <div class="flex justify-between items-baseline">
                        <h3 class="text-2xl text-white font-serif italic group-hover:text-rebecca-purple transition-colors">Chrome Void</h3>
                        <span class="text-sm text-dark-pastel-red">Cinema 4D / Octane</span>
                    </div>
                </div>

                <!-- Project 3 -->
                <div class="project-card group cursor-pointer">
                    <div class="hover-image-container relative aspect-[16/9] w-full bg-gray-900 mb-4 rounded-sm overflow-hidden">
                        <img src="https://images.unsplash.com/photo-1614850523459-c2f4c699c52e?q=80&w=1000&auto=format&fit=crop" alt="Surreal Landscape" class="hover-image w-full h-full object-cover opacity-80 group-hover:opacity-100">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end p-6">
                            <span class="text-white font-sans text-sm tracking-widest uppercase border border-white/30 px-3 py-1 backdrop-blur-md">View Project</span>
                        </div>
                    </div>
                    <div class="flex justify-between items-baseline">
                        <h3 class="text-2xl text-white font-serif italic group-hover:text-rebecca-purple transition-colors">Liquid Dreams</h3>
                        <span class="text-sm text-dark-pastel-red">Houdini / Redshift</span>
                    </div>
                </div>

                <!-- Project 4 -->
                <div class="project-card group cursor-pointer md:mt-12">
                    <div class="hover-image-container relative aspect-[3/4] w-full bg-gray-900 mb-4 rounded-sm overflow-hidden">
                        <img src="https://images.unsplash.com/photo-1558655146-d09347e92766?q=80&w=1000&auto=format&fit=crop" alt="Cyberpunk Portrait" class="hover-image w-full h-full object-cover opacity-80 group-hover:opacity-100">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end p-6">
                            <span class="text-white font-sans text-sm tracking-widest uppercase border border-white/30 px-3 py-1 backdrop-blur-md">View Project</span>
                        </div>
                    </div>
                    <div class="flex justify-between items-baseline">
                        <h3 class="text-2xl text-white font-serif italic group-hover:text-rebecca-purple transition-colors">Automata</h3>
                        <span class="text-sm text-dark-pastel-red">ZBrush / Maya</span>
                    </div>
                </div>

            </div>
        </section>

        <!-- About Section -->
        <section id="about" class="py-24 px-4 md:px-12 lg:px-24 relative overflow-hidden">
            <!-- Decorative Background Text -->
            <div class="absolute top-0 left-0 w-full h-full overflow-hidden pointer-events-none opacity-5">
                <span class="text-[20vw] font-serif text-white leading-none whitespace-nowrap absolute top-10 left-0 animate-marquee">SCHUY SCHUY SCHUY</span>
            </div>

            <div class="max-w-4xl mx-auto glass-panel p-8 md:p-16 rounded-sm relative z-10">
                <h2 class="font-serif text-4xl md:text-5xl text-white mb-8">About The Artist</h2>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                    <div class="md:col-span-1">
                        <div class="w-full aspect-square bg-gray-800 grayscale hover:grayscale-0 transition-all duration-500 overflow-hidden rounded-sm">
                            <img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?q=80&w=1000&auto=format&fit=crop" alt="Artist Portrait" class="w-full h-full object-cover">
                        </div>
                    </div>
                    <div class="md:col-span-2 space-y-6 text-lg">
                        <p>
                            I am a visual designer obsessed with the intersection of technology and emotion. Using tools like Blender, Houdini, and Cinema 4D, I explore surrealism in the digital age.
                        </p>
                        <p>
                            My work is heavily inspired by automotive design, cyberpunk aesthetics, and classical composition.
                        </p>
                        
                        <div class="pt-6">
                            <h4 class="font-sans text-xs tracking-widest uppercase text-rebecca-purple mb-4">Software Stack</h4>
                            <div class="flex flex-wrap gap-3">
                                <span class="border border-white/20 px-3 py-1 rounded-full text-xs hover:bg-white hover:text-black transition-colors">Blender</span>
                                <span class="border border-white/20 px-3 py-1 rounded-full text-xs hover:bg-white hover:text-black transition-colors">Cinema 4D</span>
                                <span class="border border-white/20 px-3 py-1 rounded-full text-xs hover:bg-white hover:text-black transition-colors">Octane</span>
                                <span class="border border-white/20 px-3 py-1 rounded-full text-xs hover:bg-white hover:text-black transition-colors">Substance</span>
                                <span class="border border-white/20 px-3 py-1 rounded-full text-xs hover:bg-white hover:text-black transition-colors">After Effects</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Footer / Contact -->
        <footer id="contact" class="py-24 px-4 md:px-12 lg:px-24 border-t border-white/10 bg-black relative">
            <div class="flex flex-col md:flex-row justify-between items-start md:items-end gap-12">
                <div>
                    <h2 class="font-serif text-6xl md:text-8xl text-white mb-4">Let's Create.</h2>
                    <p class="text-old-silver max-w-md mb-8">Available for freelance projects and collaborations. Drop a line if you want to build something impossible.</p>
                    <a href="mailto:hello@schuy.xyz" class="text-2xl text-rebecca-purple hover:text-dark-pastel-red transition-colors underline decoration-1 underline-offset-8">hello@schuy.xyz</a>
                </div>
                
                <div class="flex flex-col gap-4 text-right w-full md:w-auto">
                    <a href="#" class="text-white hover:text-rebecca-purple transition-colors uppercase tracking-widest text-sm">Instagram</a>
                    <a href="#" class="text-white hover:text-rebecca-purple transition-colors uppercase tracking-widest text-sm">Twitter / X</a>
                    <a href="#" class="text-white hover:text-rebecca-purple transition-colors uppercase tracking-widest text-sm">ArtStation</a>
                    <a href="#" class="text-white hover:text-rebecca-purple transition-colors uppercase tracking-widest text-sm">Behance</a>
                </div>
            </div>
            
            <div class="mt-24 pt-8 border-t border-white/5 flex justify-between items-center text-xs text-gray-600 uppercase tracking-widest">
                <span>&copy; 2024 Schuy.xyz</span>
                <span>Made with Three.js</span>
            </div>
        </footer>

    </main>

    <script>
        // Initialize Lucide Icons
        lucide.createIcons();

        // --- THREE.JS BACKGROUND ANIMATION ---
        const initThreeJS = () => {
            const canvas = document.querySelector('#webgl-canvas');
            const renderer = new THREE.WebGLRenderer({ canvas, alpha: true, antialias: true });
            renderer.setSize(window.innerWidth, window.innerHeight);
            renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

            const scene = new THREE.Scene();
            
            // Fog to blend into background
            scene.fog = new THREE.FogExp2(0x090304, 0.002);

            // Camera
            const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            camera.position.z = 5;

            // Lighting derived from palette
            const ambientLight = new THREE.AmbientLight(0x090304, 2);
            scene.add(ambientLight);

            const pointLight1 = new THREE.PointLight(0x5D2896, 2, 10); // Rebecca Purple
            pointLight1.position.set(2, 2, 2);
            scene.add(pointLight1);

            const pointLight2 = new THREE.PointLight(0xBF491E, 2, 10); // Dark Pastel Red
            pointLight2.position.set(-2, -2, 2);
            scene.add(pointLight2);

            // Geometry: Wireframe Icosahedron
            const geometry = new THREE.IcosahedronGeometry(1.5, 1);
            const material = new THREE.MeshStandardMaterial({ 
                color: 0x1a1a1a,
                roughness: 0.1,
                metalness: 0.8,
                wireframe: true
            });
            const mesh = new THREE.Mesh(geometry, material);
            scene.add(mesh);

            // Particles
            const particlesGeometry = new THREE.BufferGeometry();
            const particlesCount = 700;
            const posArray = new Float32Array(particlesCount * 3);

            for(let i = 0; i < particlesCount * 3; i++) {
                posArray[i] = (Math.random() - 0.5) * 15;
            }

            particlesGeometry.setAttribute('position', new THREE.BufferAttribute(posArray, 3));
            const particlesMaterial = new THREE.PointsMaterial({
                size: 0.02,
                color: 0x7E8489, // Old Silver
                transparent: true,
                opacity: 0.8,
            });
            const particlesMesh = new THREE.Points(particlesGeometry, particlesMaterial);
            scene.add(particlesMesh);

            // Mouse Interaction
            let mouseX = 0;
            let mouseY = 0;
            let targetX = 0;
            let targetY = 0;

            const windowHalfX = window.innerWidth / 2;
            const windowHalfY = window.innerHeight / 2;

            document.addEventListener('mousemove', (event) => {
                mouseX = (event.clientX - windowHalfX);
                mouseY = (event.clientY - windowHalfY);
            });

            // Animation Loop
            const clock = new THREE.Clock();

            const animate = () => {
                targetX = mouseX * 0.001;
                targetY = mouseY * 0.001;

                const elapsedTime = clock.getElapsedTime();

                // Rotate main mesh
                mesh.rotation.y += 0.5 * (targetX - mesh.rotation.y);
                mesh.rotation.x += 0.05 * (targetY - mesh.rotation.x);
                mesh.rotation.z += -0.05 * (targetY - mesh.rotation.x);
                
                // Gentle constant rotation
                mesh.rotation.y += 0.002;
                mesh.rotation.x += 0.001;

                // Rotate particles
                particlesMesh.rotation.y = -mouseX * 0.0002;
                particlesMesh.rotation.x = -mouseY * 0.0002;
                particlesMesh.rotation.z += 0.0005;

                // Pulse lights
                pointLight1.intensity = 2 + Math.sin(elapsedTime * 2) * 0.5;
                pointLight2.intensity = 2 + Math.cos(elapsedTime * 1.5) * 0.5;

                renderer.render(scene, camera);
                requestAnimationFrame(animate);
            };

            animate();

            // Resize Handler
            window.addEventListener('resize', () => {
                camera.aspect = window.innerWidth / window.innerHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(window.innerWidth, window.innerHeight);
            });
        };

        // --- GSAP ANIMATIONS ---
        const initAnimations = () => {
            gsap.registerPlugin(ScrollTrigger);

            // Remove loader
            const loader = document.getElementById('loader');
            gsap.to(loader, {
                opacity: 0,
                duration: 1,
                delay: 0.5,
                onComplete: () => loader.style.display = 'none'
            });

            // Hero Text Reveal
            gsap.from('.reveal-text', {
                y: 50,
                opacity: 0,
                duration: 1.5,
                stagger: 0.2,
                ease: "power4.out",
                delay: 1
            });

            // Project Cards Scroll Reveal
            gsap.utils.toArray('.project-card').forEach((card, i) => {
                gsap.from(card, {
                    scrollTrigger: {
                        trigger: card,
                        start: "top 85%",
                        toggleActions: "play none none reverse"
                    },
                    y: 100,
                    opacity: 0,
                    duration: 1.2,
                    ease: "power3.out"
                });
            });

            // Marquee effect for background text
            gsap.to(".animate-marquee", {
                xPercent: -50,
                repeat: -1,
                duration: 20,
                ease: "linear"
            }).totalProgress(0.5);
        };

        // Start everything when DOM is ready
        document.addEventListener('DOMContentLoaded', () => {
            initThreeJS();
            initAnimations();
        });

    </script>
</body>
</html>
